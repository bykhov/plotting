# LaTeX build artifacts
*.aux
*.log
*.toc
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.synctex(busy)
*.bbl
*.blg
*.idx
*.ilg
*.ind
*.lof
*.lot
*.bcf
*.run.xml
*.xdv
*.txt

# lwarp intermediates (lwarp.css is generated but deployed, so it is tracked)
*.lwarpmkconf
lwarpmk.conf
plotting_html.*
plotting-images.txt
*_html_inc.tex
lwarp.ist
lwarp.xdy
*.tmp
*.cut
*.sidetoc

# The raw PDF is ignored; the compressed plottingc.pdf (compress.bat) is tracked and deployed
plotting.pdf

# Raster exports (regenerated by the scripts; the HTML uses SVG)
*.jpeg
*.png

# Python
__pycache__/

# Staging directory (created by GitHub Actions)
_site/

# OS / editor
Thumbs.db
.DS_Store
*.swp
*~
/.claude/settings.local.json
/.idea
